// This is a placeholder script that simply succeeds.
// Since we're deploying static files directly from the public directory,
// we don't need any build process.
console.log('Deploying static files from public directory...');
process.exit(0); 